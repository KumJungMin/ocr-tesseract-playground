import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ocr-tesseract-playground',
  webDir: 'dist'
};

export default config;
